version: "3.3"

services:
  db:
    image: postgres:14.0-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - db.env
    container_name: postgres
    ports:
      - 5432:5432
    restart: always
